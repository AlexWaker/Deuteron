[package]
name = "solgent-agent"
version = "0.1.0"
edition = "2021"

[dependencies]
keyring = { version = "3.6.3", features = ["apple-native"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[profile.release]
codegen-units = 1
lto = true
strip = true